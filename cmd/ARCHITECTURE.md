# Command architecture

Commands parse process-level intent, load configuration, and delegate lifecycle ownership to internal application packages. Business rules and provider protocols do not live in command entry points.
