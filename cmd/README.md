# Commands

Executable entry points live in child directories so command wiring remains separate from reusable application code.

- `ARCHITECTURE.md` documents the command-layer boundary and startup responsibilities.
