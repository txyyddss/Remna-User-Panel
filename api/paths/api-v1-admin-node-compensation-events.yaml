get:
  tags: [Admin]
  operationId: listNodeCompensationEvents
  summary: List ongoing and historical node outages
  parameters:
    - { name: status, in: query, schema: { type: string, enum: [observing, pending_review, queued, dismissed, ineligible] } }
    - { name: cursor, in: query, schema: { type: string } }
    - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 25 } }
  responses:
    '200':
      content: { application/json: { schema: { $ref: '../components/schemas/schemas-22.yaml#/NodeCompensationEventPage' } } }
    '403': { $ref: '../components/responses.yaml#/Forbidden' }
