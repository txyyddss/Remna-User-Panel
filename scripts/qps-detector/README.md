# QPS detector node reporter

- `install.sh` installs or removes the Debian 13 locked cron reporter, keeps its node token root-only, captures live `xlogs` output for the configured interval, and uploads privacy-sensitive logs in 15 MiB bounded batches.
