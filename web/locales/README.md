# Product language catalog

English and Simplified Chinese are split into matching domain JSON files so
copy stays reviewable and below the repository line ceiling. `manifest.json`
declares the supported locales and ordered domains. Vite validates key and
placeholder parity on every build.
