# Product language catalog

English and Simplified Chinese are split into matching domain JSON files so
copy stays reviewable and below the repository line ceiling. `home.json` also owns the adjustable UTC per-node traffic labels. `manifest.json`
declares the supported locales and ordered domains, including Home and the
visual onboarding editor. Vite validates key and placeholder parity on every
build.
