# Activity admin

- `AdminActivitySettings.vue` edits the calendar timezone, daily check-in reward range, and group-message reward configuration from Settings.
- `AdminLuckyDrawEditor.vue` validates weighted prize configurations.
