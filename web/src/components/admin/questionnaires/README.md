# Questionnaire admin

- `QuestionnaireImportWorkflow.vue` previews imports and queues idempotent settlement.
- `QuestionnaireImportWorkflow.vue` owns upload, preview, settlement, and completion presence while keeping the drawer itself library-owned.
