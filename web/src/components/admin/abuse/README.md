# Abuse administration UI

- `AbusePolicyCard.vue` edits the revisioned global QPS policy and punishment ladder.
- `AbuseRulesCard.vue` manages RE2 domain reasons and a compact remote-ID whitelist.
- `AbuseNodesRecordsCard.vue` exposes encrypted node-key controls, metric summary, desktop records, and touch-readable mobile rows.
