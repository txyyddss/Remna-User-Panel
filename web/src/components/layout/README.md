# Layout components

- `AppShell.vue` owns the fullscreen safe-area shell, member navigation, safe content focus, and Telegram back navigation.
- `LanguageControl.vue` provides the icon-only bottom language selector and its accessible locale popover.
- `AppShell.test.ts` verifies skip-link focus and Telegram BackButton behavior.
