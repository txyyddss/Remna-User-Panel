# Layout components

- `AppShell.vue` owns responsive member navigation, safe content focus, and Telegram back navigation.
- `AppShell.test.ts` verifies skip-link focus and Telegram BackButton behavior.

