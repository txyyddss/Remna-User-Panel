# Catalog components

- `CatalogPage.vue` composes catalog loading, selection, and checkout states.
- `ComboOption.vue` and `SquadSelector.vue` provide selectable plan controls.
- `CatalogCheckout.vue` reviews the server quote before purchase.
- `ComboOption.test.ts` verifies stable plan values and selection events.

