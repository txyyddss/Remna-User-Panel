# Draw artwork

- `gift-box.json` is a self-authored Lottie vector box opening.
- `celebration.json` is a self-authored Lottie vector reward halo.

Both play once from local assets and never carry prize-selection logic. Reduced-motion rendering uses the static receipt instead.
