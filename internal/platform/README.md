# Platform modules

Platform child packages provide persistence, configuration, identifiers, encryption, durable work processing, backups, and bounded upstream-call queues without owning business policy.

- `ARCHITECTURE.md` describes the platform layer's runtime and dependency boundaries.
