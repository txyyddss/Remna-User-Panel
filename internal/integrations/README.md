# Provider integrations

Child packages implement narrow, typed adapters for external providers. Provider credentials, wire validation, bounded response handling, and sanitized errors stay inside those adapters.

- `ARCHITECTURE.md` defines the common provider-adapter boundary and verification expectations.
